Clone the application from github.
write the command to run the application:
npm run dev
If the port is different from yours you can change the port on row 1 in api.js to the correct backend port.
remember to run the backend first.